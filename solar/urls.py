# solar/urls.py

from django.urls import path

urlpatterns = [
    # Esta lista está vazia, o que significa que este arquivo de URLs
    # não define nenhum padrão de URL diretamente.
    # O Django precisa que a variável 'urlpatterns' exista e seja iterável.
]
